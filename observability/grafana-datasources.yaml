# Provisioned automatically on Grafana startup -- no manual "add data
# source" click-through needed.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
